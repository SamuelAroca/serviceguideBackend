# Production overrides - activate with SPRING_PROFILES_ACTIVE=prod
# ddl-auto=validate instead of update: the schema must already match the
# entities (created via a migration or a prior "update" run); the app
# should fail fast on drift instead of altering the live schema at boot.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
